# ============================================
# SERVICE - Exposición de red
# ============================================
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: order-service
  labels:
    app: order-service

spec:
  type: NodePort
  selector:
    app: order-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8083
      nodePort: 30083